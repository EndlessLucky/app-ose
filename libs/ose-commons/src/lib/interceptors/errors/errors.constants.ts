export const statusErrors: Map<number, string> = new Map([
  [ 401, 'Usted no está autorizado' ],
  [ 404, 'El servicio no se encontro' ],
]);

export const codeErrors: Map<number, string> = new Map([
  [ 50, 'el texto puede tener maximo 500 caracteres' ],
]);
